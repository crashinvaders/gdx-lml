org.gradle.daemon=true
org.gradle.jvmargs=-Xms128m -Xmx1500m
org.gradle.configureondemand=false

# Your jar signing data:
#signing.keyId= 
#signing.password= 
#signing.secretKeyRingFile= 

# Central Portal user-token credentials. Generate a user token at
# https://central.sonatype.com/usertoken and keep actual values outside VCS.
# The legacy ossrh names are retained for compatibility with this build.
ossrhUsername=
ossrhPassword=
